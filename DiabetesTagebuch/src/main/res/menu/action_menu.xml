<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_grunddaten"
        android:icon="@drawable/bluecircle"
        android:title="@string/menu_grunddaten"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_bzliste"
        android:title="BZ-Liste"
        app:showAsAction="never"/>
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_sync"
        android:title="@string/menu_sync"
        android:icon="@drawable/sync_black_24x24"
        app:showAsAction="ifRoom"/>

</menu>